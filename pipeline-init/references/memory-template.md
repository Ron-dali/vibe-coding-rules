## V2.6 流水线强制纪律

> 此文件由 pipeline-init Step -1.a Memory 自愈机制读取并写入用户 Memory 文件。
> 每个 Section 末尾的 `<!-- V2.6_PIPELINE_MARKER -->` 是自愈检测的锚点，不可删除。

### Skill依赖链（不可跳过顺序）

改代码前 → coding-principles（三层体系：前置门→六大原则→三大陷阱）→ safe-terminal-executor（如需API/终端验证）→ 改代码 → self-check（28条规则 + 步骤5.5完整性阻断点 + 步骤6强制实测）→ web-testing（自动化测试，阻断点通过后强制执行）→ changelog（变更日志 + 生长检测）→ git commit → ✅ 交付闭环

<!-- V2.6_PIPELINE_MARKER -->

### 强制自检硬规则

- **步骤5.5（完整性阻断点）**：纸面步骤0-6.5全部完成 → 逐项勾选确认 → 步骤6强制门禁 → 不通过则阻断后续操作
- **步骤6（测试）不可跳过**：纸面检查通过不算自检完成，必须实际跑验证（web-testing 或手动验证）
- **跳过步骤6直接写 changelog 或 git commit = 纸面自检伪完成，禁止**

<!-- V2.6_PIPELINE_MARKER -->

### 开发文档标准路径

```
开发文档/
├── 00-必读/         ← 规范契约（每次会话必加载）
├── 01-变更日志/     ← 变更日志-YYYY-MM-DD.md
├── 04-方案设计/     ← 方案文档
├── 待办事项/        ← YYYY-MM-DD.md 每日待办
└── 归档/            ← 按季度归档
```

所有开发文档表头必须包含：`> 📋 管理规范见: 开发文档/00-必读/开发文档管理.md`

<!-- V2.6_PIPELINE_MARKER -->

### 🍞面包屑规则

每次修改文件必须补设/更新文件头部的 🍞 AI面包屑导航块：
- `@COUPLED`：联动文件（改这里必须同步改哪些文件）
- `📖`：开发文档引用（详细设计文档路径）
- `@BUGFIX`：Bug 修复记录（日期+原因+方案）

面包屑格式：内嵌标签含义速查表 + 联动文件列表 + 文档引用，AI 无需加载 Skill 即可理解。

<!-- V2.6_PIPELINE_MARKER -->

### 项目初始化状态

已初始化：YYYY-MM-DD | V2.6.0

<!-- V2.6_PIPELINE_MARKER -->
